import SwiftUI
import SwiftData

struct TasksView: View {
    
    var body: some View {
        List {
        }
        .navigationTitle("tasks")
        .navigationBarTitleDisplayMode(.large)
    }
}
